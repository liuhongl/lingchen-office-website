# 灵宸官网项目规则

## 交流与工程原则

- 所有回复使用中文，并在开头称呼用户为「[利哥]」。
- 尊重事实和设计证据，不用“看起来差不多”代替验证。
- 只修改当前任务直接涉及的文件和区域，不顺手重构无关代码。
- 修改后必须执行合理验证；无法验证时明确说明原因和风险。

## 设计稿还原任务

凡涉及 MasterGo 页面实现、视觉修改或设计稿对比，必须使用项目 Skill：

`/.agents/skills/mastergo-pixel-implementation/SKILL.md`

### 可执行门禁（优先于完成声明）

- 每页除 `scope.md`、`content-ledger.md`、`final-validation.md` 外，必须有 `visual-ledger.md`；视觉台账必须逐元素记录精确字体/字重、背景、渐变、图标来源和特殊图形。
- 文案或视觉台账存在 `BLOCKED`、`待核对`、`未完成` 时，禁止实现对应区块；上一块未完成内容与视觉双验收，禁止进入下一块。
- 公共 Header/Footer/Logo/字体开工前记录基线截图和文件 SHA-256，交付时复核；未经授权的哈希变化即失败。
- “统一按钮/渐变风格”只允许用于 MasterGo 明确复用同一组件的实例，不能覆盖设计稿中的具体差异。
- 最终工程命令后必须运行 `pnpm mastergo:check <page-slug>`；该命令未通过时只能报告实际门禁状态，不得报告 1:1。

### MasterGo 图标与图片素材

本官网语义图标的持续授权与证据要求同时遵守：

`/docs/semantic-icon-generation-rules.md`

- 设计稿中存在明确图标、插画、Logo 或特殊图形时，必须从 MasterGo 对应原始图层直接导出，不得从整页截图或局部截图裁切。
- 矢量图标优先导出 SVG，保持路径、描边、圆角、渐变和透明度；不得先转成小尺寸 PNG，也不得把 SVG 栅格化后再使用。
- 只有原始素材本身是位图或 SVG 无法正确表达时才使用 PNG/WebP。位图导出尺寸至少为 CSS 实际显示尺寸的 2 倍；高密度素材优先 3 倍，并在页面中明确设置 CSS 宽高，禁止浏览器放大低分辨率图片。
- 禁止用 MasterGo 缩略图、聊天截图、页面截图裁图、Emoji、Unicode 符号、Lucide 或其他图标库替代设计稿中可识别、可导出的正式图标。
- 仅当 MasterGo 明确使用同一图标库图标，或原图层不可导出且用户确认允许替代时，才可使用图标库；必须在 `visual-ledger.md` 记录精确组件名、尺寸、stroke width、颜色和例外依据。
- 只有视觉真源不存在对应正式图标、且用户明确授权按业务语义生成时，才允许制作独立语义 SVG；该授权不得替代可导出的 MasterGo 原始图层。必须记录业务语义、参考风格、`viewBox`、描边、颜色、CSS 尺寸、文件路径和 SHA-256，并避免机器人、大脑、魔法棒或无语义闪光等模板化“AI 味”符号。
- CSS 只用于设计稿中本身属于基础几何的圆点、直线、纯色圆角块等；品牌图形、复合图标、手绘路径和带细节的特殊图形不得用近似 CSS 重画。
- 每个导出素材必须记录 MasterGo `layer_id`、导出格式、源尺寸、CSS 显示尺寸、DPR 和文件路径，并以原始尺寸单独打开检查透明区域、裁切完整性、居中和清晰度。
- MasterGo 网页下载按钮不可用或未产生下载事件时，允许直接提取该图层“资源”面板自身生成的 SVG 源码、原始文件数据或 2×/3× 导出数据；必须证明数据来自当前选中图层，并记录提取方式、实际像素/`viewBox`、CSS 显示尺寸、有效 DPR 和 SHA-256。资源面板缩略预览、设计画布截图和放大后的单倍位图不属于原始导出数据。
- 自动提取后必须检查文件头、真实尺寸、透明边界和非透明内容；若提取数据的有效 DPR 小于 2、SVG 路径不完整、资源与当前图层无法一一对应，仍标记为 `BLOCKED`，不得通过插值放大、矢量描摹或近似重画规避门禁。
- 无法取得 MasterGo 原始图层或正式素材时，该图标标记为 `BLOCKED`，不得先放近似图标占位后继续宣称区块完成；只有已确认视觉真源不存在对应正式素材且用户明确授权语义生成时，才按上条例外执行。

### 官网 SEO / GEO 门禁

所有新增或修改的可索引页面必须同时遵守：

`/docs/seo-geo-guidelines.md`

- 开工前复制 `mastergo-pixel-implementation/templates/seo-geo-validation.md` 到对应 `design-baselines/<page>/`，建立逐页 SEO/GEO 台账。
- 每页必须有独立且准确的 title、description、自指 canonical、唯一 h1、合理标题层级和索引策略；禁止子页错误继承首页 canonical。
- 公司、产品、能力、案例、指标和 FAQ 必须来自已确认事实；禁止模型补写、夸大或伪造引用。
- 关键内容必须存在于服务端 HTML；结构化数据只能描述页面可见内容或已有正式证据，不得作为隐藏营销文案。
- 新正式页面必须核对 sitemap、robots、站内入口、状态码和结构化数据。
- SEO/GEO 不得擅改页面已确认的内容真源文案或新增可见模块；如需新增，先经用户确认并建立内容与视觉基线。
- 视觉 1:1、SEO/GEO、工程验证是三个独立门禁，必须分别汇报。
- 本地完成只能报告“SEO/GEO 已实现，待线上验证”；未完成线上抓取、索引和生成式答案抽样时，不得报告 GEO 效果已验证。

### 公共预约弹窗与转化按钮

- `BookingModal` 是全站唯一的预约、演示、体验与合作咨询弹窗；`BookingTrigger` 是页面 CTA 的默认触发入口。
- “立即体验”“预约演示”“预约产品演示”“预约产品演示与体验”“立即预约”等表达预约、体验、演示或合作咨询意图的按钮，必须使用 `BookingTrigger` 打开公共 `BookingModal`。
- 统一的是交互行为和弹窗内容，不是按钮的可见文案或视觉。按钮文字取自当前页面的内容真源，颜色、渐变、尺寸、图标和位置取自当前页面的视觉真源。
- 新页面不得复制 `BookingModal`、另建同类表单、在页面中重复维护弹窗内容，或自行创建另一套预约状态与提交提示。
- 只有设计稿和产品流程明确要求跳转到独立页面、第三方地址、下载文件或执行其他非预约动作时，才允许不使用 `BookingTrigger`；例外必须记录在页面 `scope.md`。
- 修改公共弹窗字段、产品选项、校验、提交行为、成功/失败状态或视觉属于全站变更，必须获得用户明确授权并回归所有触发入口。
- 页面验收必须逐个点击全部预约类 CTA，确认打开的是同一个 `BookingModal`，并验证关闭按钮、遮罩、Escape、滚动锁定和键盘焦点。移动端显示仅在移动端重新纳入范围后验收。

### 内容真源与视觉真源

内容与视觉必须分别声明真源，不得再用单一“1:1 设计稿还原”口径混用两者。

内容真源优先级：

1. 用户在当前对话中明确确认的覆盖要求。
2. 用户指定并确认版本的交互原型；它决定页面模块、顺序、可见文案、指标和交互内容。
3. 用户提供的正式素材和文章原文。
4. MasterGo 文案，仅用于原型缺失且用户未另行指定的内容。
5. 现有代码只能作为迁移参考，不能反向覆盖已确认原型。

视觉真源优先级：

1. 已冻结的公共 `SiteHeader`、`SiteFooter`、Logo、字体、`BookingModal` 与全站基础设计系统。
2. 用户为当前页面指定的 MasterGo 画板、现有已实现页面或既有组件风格。
3. 原型仅在视觉真源没有覆盖结构或交互状态时提供补充参考，不要求复刻其视觉样式。

每个页面开工前必须在 `scope.md` 分别记录内容真源与视觉真源、版本或访问时间、用户覆盖项。发生冲突时按对应真源的优先级执行；无法判断时停止相关区域并请用户确认。

### 官网当前桌面端范围

全部正式官网页面必须遵守：

`/docs/website-desktop-only-scope.md`

- 用户已于 2026-07-22 明确确认整个官网暂不考虑移动端；当前只实现和验收桌面端。
- 不建立移动端基线，不做移动端截图、交互或视觉结论，也不得宣称移动端已经完成。
- 已有响应式代码和历史移动端证据可以保留，但不作为当前交付结论；不得为了桌面端顺手破坏或清理既有响应式代码。
- 每页 `scope.md` 与 `final-validation.md` 必须记录移动端未纳入当前官网范围，移动端不作为桌面端冻结的阻塞项。

### 产品专项解决方案原型迁移批次规则

`/solutions/products/` 下的 Sales in、Social Grow、Mine GEO、AI获客Harness、Recov AI、DeepDoc、DeepLaw 必须同时遵守：

`/docs/product-solution-prototype-migration-rules.md`

- 内容以用户指定原型为准，视觉沿用冻结公共组件、现有官网设计系统和可用的 MasterGo 正式图层。
- 当前批次与其他官网页面一样执行 `/docs/website-desktop-only-scope.md`，不再维护只覆盖七个页面的移动端例外。
- MasterGo 中存在正式图标时仍必须导出原始图层；只有不存在对应正式素材且用户明确授权时，才允许按业务语义制作 SVG。
- 官网级语义 SVG 生成权限已记录在 `/docs/semantic-icon-generation-rules.md`；本批次直接适用，不再维护只覆盖七个页面的局部授权。
- 用户授权生成的 SVG 必须参考现有 MasterGo 图标语言，避免机器人、大脑、魔法棒和无语义闪光等“AI 味”符号，并在视觉台账记录语义、尺寸、描边、颜色、路径和 SHA-256。
- 页面专项删除、排序、纠错和布局覆盖只写入当前页面台账，不得自动套用到其他产品专项页面。

### MasterGo 标准交付流程（强制门禁）

所有后续设计稿页面必须按以下顺序执行。任何门禁未通过，不得进入下一阶段；不得为了赶进度并行跳过内容提取、逐字校验或视觉验收。

#### G0：范围与公共资产门禁

开始前必须记录：

- 页面名称、内容真源链接与版本、视觉真源链接或现有实现路径；视觉真源为 MasterGo 时记录 `page_id` / `layer_id`。
- 本次目标视口、允许修改的页面和区块。
- 公共 `SiteHeader`、`SiteFooter`、Logo、字体和其他共享组件是否已定稿。
- 用户明确确认的内容覆盖、视觉例外或替换文案。

公共头尾已定稿时视为冻结资产，新页面只复用，默认只允许修改导航高亮。范围或冲突不明确时先暂停确认。

#### G1：设计证据门禁

在写代码前，分别固定内容真源快照和视觉真源基线。视觉真源为 MasterGo 时，将目标画板切到 100% 缩放，按区块编号保存无选中框、标尺或编辑器遮挡的设计基线。每个区块必须建立证据记录，至少包含：

- 所有可见文案的逐字原文、明确换行和文本节点数量。
- 长文案的首句、末句、完整句数和完整标点。
- 容器宽高、位置、对齐、内外间距和背景作用范围。
- 字体、字号、字重、行高、字间距、颜色和渐变作用字符。
- 每侧边框、圆角、阴影以及圆点、短弧、轨道、连线、图标等特殊图形。
- 默认、悬停、聚焦、展开等交互状态。

看不清、被遮挡或无法确认的属性必须标记为 `BLOCKED` 并继续放大或获取素材，不得猜测。

#### G2：文案台账门禁

实现前必须建立逐区文案台账，格式至少包含：

| 文本 ID | 区块 | 内容真源逐字原文 | 用户确认覆盖 | 代码位置 | 核对状态 |
|---|---|---|---|---|---|

执行规则：

- 内容真源已有文案必须原样录入；禁止概括、缩写、润色、纠错、同义改写或根据上下文补全。
- 保留标点、数字、单位、英文大小写、英文空格、括号形式和明确换行。
- 只有用户明确确认的替换文案可以覆盖内容真源，并必须在台账中记录。
- 文案看不清时保持 `BLOCKED`，不得用“意思差不多”的文本代替。
- 台账未完成时不得开始该区块的 JSX 或样式实现。

#### G3：单区块实现门禁

- 一次只实现一个可独立验收的区块，不得先批量搭完整页再回头补证据。
- 先按文案台账实现结构和内容，再实现视觉样式。
- 禁止新增内容真源和用户确认内容之外的文案、卖点、指标、徽标或功能；装饰只允许来自视觉真源。
- 禁止用近似图形或近似 CSS 替代设计稿中可明确识别的元素。
- 只修改当前区块直接涉及的代码，不顺手重构其他页面或公共组件。

#### G4：单区块双重验收门禁

每个区块进入下一阶段前必须同时通过：

1. **内容验收**：将 MasterGo 台账与页面实际渲染字符串逐项比对，检查增字、漏句、错字、标点、数字、空格、大小写和换行；不能只对照源码，也不能只看截图里“有文字”。
2. **视觉验收**：在相同桌面视口生成本地截图，与 100% 设计基线并排或半透明叠加，核对边界、宽高、间距、换行、字重、颜色、渐变、圆角、阴影和特殊图形。

内容和视觉任一未通过，该区块不得标记完成，也不得进入下一区块。

#### G5：全页回归门禁

所有区块完成后必须：

- 重新执行一次全页文案台账审计，确认所有文本节点都有内容真源证据和代码位置。
- 生成全页同视口截图，检查区块顺序、累计高度、背景衔接、公共头尾复用和横向溢出。
- 检查 `document.documentElement.scrollWidth === document.documentElement.clientWidth`。
- 抽查所有重复组件，确认没有因共享样式导致局部覆盖。

用户发现任意一处擅自增加、遗漏、误录或近似样式后，立即撤销当前完成结论，并将检查范围扩大到全页同类元素；不能只修用户指出的一处。

#### G6：工程与交付门禁

视觉和内容验收完成后运行：

```bash
pnpm lint
pnpm exec tsc --noEmit
pnpm build
git diff --check
```

最终汇报必须分别列出：文案审计状态、视觉验收状态、工程验证结果、未验证项和阻塞项。工程通过不能替代内容或视觉验收。

### 强制证据产物

每个 MasterGo 页面至少保留：

- 内容真源链接与版本、视觉真源链接或实现路径、适用的 `page_id` / `layer_id`、视口和范围说明。
- 分区清单及逐字文案台账。
- 各区块无编辑器干扰的 MasterGo 100% 基线。
- 各区块本地同视口截图及对比结论。
- 用户确认的设计覆盖项。
- 全页最终截图和工程验证结果。

缺少上述证据时，只能报告实际完成到哪一道门禁，不得使用“1:1”“已完整还原”或“全页已验证”。

### 修改代码前的强制步骤

1. 明确目标页面、画板或 `layer_id`，以及本次允许修改的区域。
2. 在 MasterGo 中以 100% 缩放检查目标区域；不得根据缩略图或记忆实现。
3. 按区块记录以下证据：
   - 完整原始文案和换行；长文案必须记录首句、末句和完整句数；
   - 容器尺寸、对齐方式、间距和留白；
   - 字体、字号、字重、行高和字间距；
   - 纯色、渐变的颜色、方向和作用范围；
   - 边框每一侧的颜色与粗细、圆角和阴影；
   - 图标、圆点、短弧、轨道等图形类型和尺寸；
   - 默认、悬停、聚焦、展开等交互状态。
4. 如果任一关键属性看不清，继续放大或获取素材，不得猜测。
5. 一次只完成一个可独立验收的区块，再进入下一个区块。

### 文案完整性审计

- 文案准确性是视觉还原的前置门槛。修改样式前必须先建立逐区文案清单，覆盖标题、正文、标签、指标、按钮和脚注。
- 实现后必须将 MasterGo 原文与页面实际字符串逐项对照，检查增字、漏句、错字、标点、数字、英文空格和大小写。
- 长段落不能按截图观感判断完整性；必须核对首句、末句和完整句数。卡片高度正常、页面可运行、lint 或构建通过都不能证明文案完整。
- 用户指出任意一处文案缺失或擅自增加后，必须立即触发全页文本审计，不能只修当前一句。
- 全页文本审计未完成时，只能报告“已修正已知文案”，不得报告“全页内容已验证”或“1:1 验证通过”。

### 实现约束

- 公共 `SiteHeader`、`SiteFooter` 及其全局样式在基线确认后视为冻结资产。新页面必须直接复用，不得通过页面级 CSS、`body:has(...)`、额外 class 或复制组件修改其高度、宽度、文案、Logo、按钮、背景、边框、间距和响应式行为。
- 新页面对公共头部默认只允许设置当前导航项的高亮状态；高亮应由路由或组件参数驱动，不得借机覆盖 Header 其他样式。
- 如果新设计稿中的公共头尾与已确认基线不一致，先按公共基线实现并报告冲突；只有用户明确要求全站更新时，才能修改公共组件，并必须回归验证所有已有页面。
- 禁止新增内容真源或用户明确确认范围之外的文案、卖点、指标、徽标和功能。
- 禁止用相近样式替换明确设计，例如：
  - 渐变文字替换为单色；
  - 短色弧替换为整圈边框；
  - 分段发光轨道替换为连续实线；
  - 圆点替换为编号；
  - 局部加粗替换为整段统一字重。
- 渐变必须核对起止颜色、方向和作用字符；边框必须分别核对顶部和其余三边。
- 必须优先复用用户提供的正式 Logo、图片、图表和文章素材。
- 设计图标必须使用 MasterGo 原始图层导出素材：优先 SVG；位图至少按显示尺寸 2× 导出。禁止从截图裁切小图或用相近图标库图标替代。用户授权的语义 SVG 例外必须满足本文件“MasterGo 图标与图片素材”和适用批次规则的全部证据要求。
- 不为“更好看”自行改造设计稿；本项目目标是还原，不是重新设计。

### 视觉验收

每个区块完成后必须：

1. 在与设计稿基线相同的桌面视口生成本地截图。
2. 对设计稿与本地截图进行并排或半透明叠加检查。
3. 至少核对：边界位置、宽高、间距、换行、字重、颜色、渐变、圆角、阴影和图形。
4. 可测量的关键布局偏差应控制在 2 CSS px 以内；字体抗锯齿差异不作为单独失败依据。
5. 当前整个官网只验收桌面端，按 `/docs/website-desktop-only-scope.md` 记录移动端未纳入范围；不得根据桌面设计自行猜测移动端布局，也不得生成或宣称移动端验收结论。

如果没有相同视口基线或无法执行视觉对比，只能报告“已实现，待视觉验收”，不得报告“1:1 还原完成”。

### 首页还原问题沉淀

首页还原过程中已经暴露过以下问题，后续所有页面都必须按这些经验防错：

- 不能凭缩略图、印象或“官网常见做法”补设计；必须以 MasterGo 100% 缩放和可见属性为准。
- 不能把页面可运行、构建通过、布局大致完整，当成视觉还原完成。
- 不得新增内容真源中没有的卖点、短句、指标或功能；如果文案看不清，只能标记待确认。
- 不得遗漏内容真源已有文案；长文案必须核对首句、末句和完整句数，不能只复制当前可见部分。
- 渐变文字、按钮、圆点、边框、短弧、轨道、卡片错位等细节必须逐元素核对，不能用近似 CSS 形状代替。
- 每个大模块的背景色、宽度、左右留白和上下间距必须单独量取；不能把一个 section 样式复用到所有模块。
- 交付前必须用本地截图与 MasterGo 基线同视口对比，并检查 `scrollWidth === clientWidth`，避免横向滚动条。
- 用户指出某个点后能修好，不代表整体已合格；必须回到全区块 checklist 继续自查未指出的问题。
- 公共 Header/Footer 定稿后不得在新页面中重复还原或局部覆盖；新页面通常只处理导航高亮和页面主体。

### 完成等级和汇报用语

- `已实现`：结构和样式代码完成，但未完成同视口视觉比较。
- `已视觉校准`：已完成同视口截图和逐项人工核对，仍可能存在字体渲染差异。
- `1:1 验证通过`：有相同视口设计基线、截图叠加或差异结果、逐项检查记录，并且没有未说明的明显差异。

不得将 lint、构建通过等同于视觉还原通过。

### 工程验证

视觉验收后至少运行：

```bash
pnpm lint
pnpm exec tsc --noEmit
pnpm build
git diff --check
```

任何一项失败都必须修复或明确报告，不能声称任务完成。

## 必须暂停并说明的情况

- 无法访问目标 MasterGo 页面或指定图层。
- 正式字体、Logo、图片、图表等关键素材缺失。
- 设计稿、用户确认要求和原型之间存在无法自行消解的冲突。
- 响应式设计缺失，且不同处理方式会明显改变结果。
- 用户要求 1:1，但当前无法获得相同尺寸的设计基线。
