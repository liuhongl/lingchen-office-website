# 全站字体下限最终验证

- 静态审计：`app/**/*.css` 中无显式小于 12px 的 `font-size`。
- 运行态审计：正式 sitemap 路由在桌面视口逐页检查可见 DOM/SVG 文本；研究院 DOCX 导入内容在渲染时统一应用 9pt（12px）下限。
- 横向溢出：正式 sitemap 路由逐页检查 `scrollWidth === clientWidth`。
- 默认正文仍遵循各页面既有 14px 体系；没有粗暴覆盖原有标题与大字号层级。
- 当前官网只验收桌面端，移动端未纳入视觉验收。
