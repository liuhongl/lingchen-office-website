# Social Grow 原型内容迁移台账（2026-07-22）

## 真源与覆盖

- 内容真源：Google AI Studio 当前原型 Social Grow 页 `<main>` 完整文本。
- 视觉真源：既有 Social Grow 专项解决方案页面与本目录 MasterGo 基线。
- 用户覆盖：`1A/2A/3A`；保留现有模块视觉，只同步原型模块、顺序、文字、指标与主体 CTA；明显错字修正，歧义保留原文。

## 逐区核对

| 区块 | 原型内容要点 | 本地差异与执行 | 代码位置 | 状态 |
|---|---|---|---|---|
| 01 Hero | `产品专项解决方案 · SOCIAL GROW`；标题、副标题；`知识入库 → 自媒体出库`；企业市场 / 品牌团队；预约产品演示 | 补眉题；`出稿`→`出库`；补斜杠两侧空格；保留现有 Hero 视觉与预约弹窗 | `page.tsx` | PASS |
| 02 一图读懂 | 标题使用半角括号；`≈ 0`、`1 人`、`1 → N` | 同步空格与半角括号 | 同上 | PASS |
| 03 痛点 | `按赛道实时聚合`；结果连接符为 `➔`；`内容复用率成倍~成百倍放大` | `按需`→`按赛道`；统一 `➔`；明显错字 `成级`→`成倍` | 同上 | PASS |
| 04 数字流 | 标题半角括号；步骤标题 `01 热点雷达` 至 `06 数据面板`；步骤 3 为 `一篇知识` | 标题、步骤标题和两处错词同步；保留现有竖向步骤视觉 | 同上 | PASS |
| 05 优势 | `数据面板价值`；选题说明使用顿号；图谱标签与说明使用带空格 `➔` | 同步文案和箭头；复用现有正式 MasterGo SVG | 同上 | PASS |
| 06 成果 | 原型风险说明结尾无句号 | 按 3A 将缺失句号视为明显标点错误，保留本地句号 | 同上 | PASS |
| 07 场景 | 表头 `业务模块 / 场景类型`；律所市场负责人、律所知识库；企业专业 IP 高频获客；标题 `典型落地场景详解` | 逐字同步；原型第二行多余 `//` 视为明显排版错误删除 | 同上 | PASS |
| 08 合作 | `可按照使用量或者结果付费` | `可按`→`可按照` | 同上 | PASS |
| 09 选择理由与 Closing | `为什么选择 Social Grow？`；Closing 仅标题与说明，无 CTA | 问号全角；移除本地多余 Closing CTA | 同上 | PASS |

## 原型歧义与错字处理

| 项目 | 原型 | 处理 | 依据 |
|---|---|---|---|
| 内容复用率 | `成倍~成百倍放大` | 原样采用 | 语义明确；修正本地明显错字 `成级` |
| 场景 2 前缀 | `// 落地业务收益：` | 删除 `//`，保留 `落地业务收益：` | 明显排版残留，不属于业务文案 |
| 风险说明句号 | 原型末尾缺 `。` | 保留完整句号 | 明显漏标点 |
| 关闭 CTA | 原型无预约按钮 | 删除本地按钮 | 原型决定主体 CTA |

## 图标台账结论

- 现有对应模块均已有从 MasterGo 原始图层导出的正式 SVG，详见 `visual-ledger.md`。
- 本轮内容同步没有新增无素材的语义图标位，因此不生成新 SVG。
