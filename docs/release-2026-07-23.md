# 官网生产发布记录（2026-07-23）

## 用户确认的发布边界

- 正式联系方式以联系页为准：`上海市普陀区金昌路1858号3幢一层`、`shenhuaibo@lingchenai.cn`、`18621786899`。
- 本次不新增隐私说明；这是用户明确接受的上线风险，不代表隐私合规已完成。
- 当前官网可见的产品能力、案例描述、指标及行业/机构描述已获公司授权公开。
- DeepDoc 本次不校验图标来源、DPR 与逐图一致性；允许上线，但不得宣称图标验收通过或页面达到 1:1。
- 当前交付只验收桌面端；移动端未纳入本次官网范围。

## 本地发布门禁

- `pnpm lint`：PASS。
- `pnpm exec tsc --noEmit`：PASS。
- `pnpm build`：PASS；生成 71 个静态页面。
- `git diff --check`：PASS。
- 正式页面/批次 MasterGo 证据检查：31 个 PASS。
- 历史 `customer-case-card-preview` 证据目录对应的预览路由已不存在，不进入构建、sitemap 或生产发布。
- 静态产物：70 个 HTML、3197 个内部引用、0 个缺失资源；除 Next.js 内部 `_not-found` 产物外，页面均为唯一 H1 和自指 canonical。
- Playwright 生产静态预览：sitemap 67 个公开路由全部 HTTP 200、唯一 H1、无横向溢出、无破损图片、存在 canonical 与 Footer。
- 公共预约弹窗：Header 触发、焦点、滚动锁定、Escape 关闭和焦点恢复通过。
- 联系表单：浏览器必填校验通过；模拟 200 返回时成功提示和表单清空通过。
- 发布前真实 API：`POST https://lingchen-ai.com/system/contact-us` 返回 HTTP 200、业务码 200；未读取或导出联系人列表。

## 生产环境只读定位

- 主机：`81.68.166.109`。
- Nginx 主配置：`/home/lingchen/mid/nginx-1.28.0/conf/nginx-web.conf`。
- Nginx 实际可执行文件：`/home/lingchen/mid/nginx-1.28.0/objs/nginx`；不是发行记录草案中曾引用的 `sbin/nginx`。
- 正式站点目录：`/home/lingchen/web/lingchen_website`。
- `/system/` 独立反向代理到现有后端；部署只替换静态站点目录，不改后端和数据库。

## 部署与回滚

1. 上传构建产物到同文件系统的临时目录。
2. 校验产物哈希、`index.html`、`robots.txt`、`sitemap.xml` 和 `favicon.ico`。
3. 将当前正式站点目录改名为带时间戳的备份目录。
4. 将临时目录原子改名为正式站点目录。
5. 执行 Nginx 配置检查；静态目录替换不主动 reload，只有配置确需变化时才 reload。
6. 若任一线上关键检查失败，将新目录移出并把备份目录原子恢复。

## 生产部署结果

- 上线代码提交：`13eebe4 feat: finalize official website release candidate`，已推送到 `origin/main`。
- 构建产物：`lingchen-website-13eebe4.tar.gz`，SHA-256 为 `057b276148b329dc2367043c0ef035c0757a615a50ea61f4f36951dd96d078ad`；上传后远端哈希一致。
- 第一次切换发现原 Nginx 仅配置 `index html/index.html;`，不兼容新静态站根目录。自动回滚成功，期间未将异常版本留在正式目录。
- Nginx 仅做必要兼容修改：`index index.html html/index.html;`；`nginx -t` 与 reload 均通过。
- Nginx 配置备份：`/home/lingchen/mid/nginx-1.28.0/conf/nginx-web.conf.backup-20260723-085457`。
- 旧站完整备份：`/home/lingchen/web/lingchen_website.backup-20260723-085607`。
- 当前正式目录：`/home/lingchen/web/lingchen_website`；目录权限与静态文件权限已校正。
- `/system/` 反向代理及现有后端未修改。

## 上线后验证结果

- `https://lingchen-ai.com/` 与 `https://www.lingchen-ai.com/` 均返回 HTTP 200。
- sitemap 中 67 个公开路由全部通过：HTTP 200、唯一 H1、无横向溢出、无破损图片、存在 canonical 与 Footer。
- 首页、联系页、DeepDoc、DeepLaw、客户案例、研究院、`robots.txt`、`sitemap.xml`、`favicon.ico` 和 `/system/contact-us` 均返回 HTTP 200；favicon 为 `image/x-icon`，大小 17470 bytes。
- 线上 Footer 联系方式与联系页一致。
- 线上公共预约弹窗通过：打开、滚动锁定、Escape 关闭、焦点恢复。
- 线上真实联系表单 POST：测试记录标记为“官网上线真实表单验收测试（2026-07-23），请忽略此条。”；HTTP 200、业务码 200，页面成功提示出现，表单清空；未读取或导出生产联系人列表。
- 表单成功状态截图：`output/playwright/release-2026-07-23/production-contact-submit-success.png`（1440×900，提交后字段已清空）。
- 生产浏览器控制台错误数：0。
- DeepDoc 图标继续按用户确认的上线例外处理：未校验图标来源、DPR 或逐图一致性，不影响本次上线，但不构成图标验收通过或 1:1 声明。

## 二次上线结果（release-2026-07-23.2）

- 上线代码提交：`7509ef9177a8f82d1715ca71b7298cd1e94baac3 feat: publish latest website refinements`，已推送到 `origin/main`。
- 发布标签：`release-2026-07-23.2`；GitHub Release：<https://github.com/liuhongl/lingchen-office-website/releases/tag/release-2026-07-23.2>。
- 构建产物：`lingchen-website-7509ef9.tar.gz`，大小 41,504,480 bytes，SHA-256 为 `678a3abb9f629767db3172c01bc4fe9dbddc047b8f502ada7d0b1588193d7587`。
- 生产服务器直连 GitHub 在 20 秒内未收到数据；本次使用 `gh-proxy.com` 仅作为 Release 资产传输代理，上传前后均按上述 SHA-256 校验，远端哈希与本地一致。
- 新版本解压到 `/home/lingchen/web/lingchen_website.release-7509ef9`，校验 `index.html`、`robots.txt`、`sitemap.xml`、`favicon.ico` 和 Nginx 配置后完成同文件系统原子切换。
- 二次上线旧站备份：`/home/lingchen/web/lingchen_website.backup-20260723-155135`；可通过目录原子切换恢复。
- 本次未修改 Nginx 配置、后端或数据库，因此无需 reload；切换后正式目录仍为 `/home/lingchen/web/lingchen_website`。
- 生产首页文件 SHA-256 为 `b180a78884d87960e7fdca9c071b170a2ca18b31d72f9ece1485e1562195a447`，与本地 `out/index.html` 一致。
- 本地门禁：32 个正式 MasterGo 页面/批次证据检查、`git diff --check`、`pnpm lint`、`pnpm exec tsc --noEmit`、`pnpm build` 全部通过；构建仍生成 71 个静态页面，sitemap 含 67 个公开 URL。
- 线上 67 个 sitemap 路由逐页通过：HTTP 200、唯一 H1、自指 canonical、Footer 存在、无横向溢出、无加载失败图片；页面错误和控制台错误均为 0。
- `lingchen-ai.com` 与 `www.lingchen-ai.com` 的首页、联系页、关于页、案例页、产品页、研究院、方案页、`robots.txt`、`sitemap.xml`、`favicon.ico` 和 `/system/contact-us` 抽样均返回 HTTP 200。
- 公共预约弹窗通过：打开后焦点进入关闭按钮、页面滚动锁定；Escape 关闭后滚动恢复且焦点回到触发按钮。
- 线上真实联系表单测试记录标记为“官网二次上线真实表单验收测试（2026-07-23，提交 7509ef9），请忽略此条。”；`POST /system/contact-us` 返回 HTTP 200、响应 `{"code":200,"msg":"操作成功","data":null}`，提交后表单与产品选择清空；未读取或导出生产联系人列表。
- 浏览器仅出现 2 条 Next.js CSS preload 未及时使用警告，不影响页面与交互；控制台错误数为 0。
- DeepDoc 图标来源/DPR/逐图一致性例外和移动端未纳入范围的边界保持不变。

## 上线后验证清单

- 首页、产品、方案、案例、研究院、联系页及详情抽样状态码。
- `robots.txt`、`sitemap.xml`、`favicon.ico`、canonical、唯一 H1。
- Header/Footer 链接和正式联系方式。
- 公共预约弹窗完整交互。
- 使用明确测试标记执行一次真实联系表单 POST，并确认前端成功状态；不读取生产联系人列表。
- 记录正式备份目录、发布目录时间和线上回滚结果。
