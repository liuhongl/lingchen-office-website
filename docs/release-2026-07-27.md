# 官网生产发布记录（2026-07-27）

## 发布边界

- 本次发布代码基线：`fbac882b1303a1f6c400af315f07fc04b10c893e`（`feat: publish July 27 website updates`）。
- 正式联系方式以联系页为准：`上海市普陀区金昌路1858号3幢一层`、`jason@alshinelaw.com`、`18621786899`。
- 本次不新增隐私说明；这是用户已确认的上线边界，不代表隐私合规已完成。
- DeepDoc 本次继续按用户确认的例外处理：不校验图标来源、DPR 与逐图一致性；允许上线，但不得据此宣称图标验收通过或页面达到 1:1。
- 当前官网只实现和验收桌面端；移动端未纳入本次发布范围。
- 全站分享图（`og:image`）继续按用户确认暂不添加；其余逐页 SEO/GEO 技术字段纳入本次门禁。

## 本地发布门禁

- `pnpm lint`：PASS。
- `pnpm exec tsc --noEmit`：PASS。
- `pnpm build`：PASS；生成 74 个静态页面。
- `git diff --check`：PASS。
- MasterGo 证据门禁：34 个正式页面/批次 PASS。
- 历史实验目录 `customer-case-card-preview` 缺少完整视觉证据，门禁未通过；该目录不对应正式路由、不进入 sitemap，也未作为本次视觉完成结论。
- sitemap 含 70 个公开路由；本地逐页浏览器审计全部通过 HTTP 200、title/description、自指 canonical、OpenGraph 核心字段、唯一 H1、`lang=zh`、有效 JSON-LD、无横向溢出、无已加载破损图片及公共头尾检查。
- 本地工程与浏览器回归发现并修复两类问题：DeepDoc/Recov AI 产品页补齐自指 OpenGraph URL；研究院 DOCX 导入文本的运行时最小字号钳制为 12px。

## 构建产物

- 本地及上传文件：`lingchen-website-fbac882.tar.gz`。
- 大小：41,597,269 bytes。
- SHA-256：`53e8e1c009b9f9f2dabfab94e7236c1a3b911b299c70ed3dfc04f1f086cd1907`。
- 服务器上传后重新计算 SHA-256，与本地一致。
- macOS 扩展属性在 Linux 解压时生成了 1,495 个 `._*` AppleDouble 元数据文件；发布前已在独立预发布目录中清理，正式站点不包含这些文件。

## 生产预检

- 生产主机：`81.68.166.109`。
- 正式站点目录：`/home/lingchen/web/lingchen_website`。
- Nginx 配置：`/home/lingchen/mid/nginx-1.28.0/conf/nginx-web.conf`。
- 当前正式站点 Nginx 实际进程：`/usr/local/nginx/sbin/nginx -c /home/lingchen/mid/nginx-1.28.0/conf/nginx-web.conf`。
- 配置中的静态根目录仍为 `/home/lingchen/web/lingchen_website`，`index index.html html/index.html;` 保持不变。
- `/system/` 反向代理保持不变；本次不修改后端、数据库或 Nginx 配置。
- 服务器磁盘可用 167 GB；发布前、切换前两次 `nginx -t` 均通过。

## 部署与回滚

- 发布包解压到独立预发布目录 `/home/lingchen/web/lingchen_website.release-fbac882`，清理 AppleDouble 文件后共有 1,362 个正式文件。
- 预发布目录已校验首页、联系页、DeepDoc 案例详情、`robots.txt`、`sitemap.xml` 与 `favicon.ico`。
- 旧站完整备份：`/home/lingchen/web/lingchen_website.backup-20260727-1017-fbac882`。
- 通过“旧目录改名为备份目录，再将预发布目录改名为正式目录”的受控切换完成上线；该过程存在极短目录切换间隔，不声明为严格原子切换。
- 静态根路径未变化，因此未执行 Nginx reload。
- 发布后发现 7 个图片文件沿用了本机 `0600` 权限，导致公共 Logo 首次线上请求返回 403；已将正式站点目录统一规范为目录 `0755`、文件 `0644`。修复后不可读文件数为 0，Logo 返回 HTTP 200。旧站备份未修改。
- 如需回滚：先将当前正式目录移出，再将上述备份目录改名恢复为 `/home/lingchen/web/lingchen_website`；恢复后重新执行关键路由与 Logo 检查。

## 上线后验证

- 裸域与 `www` 域名首页均返回 HTTP 200；线上首页 SHA-256 为 `4f21e81405c7f5045ba1a7681a224aa7003ec070c5eb91e261496d2f47e1086a`，与本地 `out/index.html` 一致。
- sitemap 中 70 个公开路由逐页通过：HTTP 200、title/description、自指 canonical、OpenGraph 核心字段、唯一 H1、有效 JSON-LD、无横向溢出、无已加载破损图片、公共 Header/Footer 与正式邮箱一致。
- 线上应用资源无 4xx/5xx，浏览器控制台错误数为 0。
- `robots.txt`、`sitemap.xml`、版本化 `favicon.ico?v=20260723-2` 均返回 HTTP 200；favicon 类型为 `image/x-icon`。
- 全站 93 个“预约/体验/商谈”入口逐一通过：打开公共 `BookingModal`、Portal 位于 `body`、滚动锁定、关闭按钮获焦、Escape 关闭、焦点恢复。
- 产品矩阵 7 张卡片悬停状态全部通过；DeepDoc 案例筛选得到 2 条结果，进入 `bank-cross-border-document-review` 后返回可恢复 `?view=product&filter=DeepDoc`；关于页 3 个品牌故事 Tab 均只显示正确面板。
- 线上真实联系表单仅提交一次：
  - 测试名称：`官网发布验收测试`。
  - 测试邮箱：`qa-launch-20260727@lingchenai.cn`。
  - 意向产品：`DeepDoc`。
  - 测试备注：`官网发布真实表单验收测试（2026-07-27），请忽略此条。`
  - `POST /system/contact-us` 返回 HTTP 200，响应 `{"code":200,"msg":"操作成功","data":null}`。
  - 页面显示“提交成功，我们会尽快与您联系”，表单状态为已验证。
  - 未读取、查询或导出生产联系人列表。
- 表单成功状态截图：`output/playwright/release-2026-07-27/production-contact-submit-success.png`。

## 发布结论

- 本次桌面端生产发布已完成，当前版本可正常访问与回滚。
- SEO/GEO 技术基础已在线验证；搜索引擎收录和生成式答案效果属于外部平台后续结果，不在本次即时上线验证范围内。
- DeepDoc 图标校验、移动端验收、全站分享图、隐私说明继续保留为已确认的非阻塞例外。

## DeepDoc 尾部留白增量发布（18c13a8）

- 发布时间：2026-07-27 11:51（Asia/Shanghai）。
- 发布代码：`18c13a8a8b8dc0266ac59744117a7f533cdd54e7`（`fix: remove DeepDoc trailing page gap`），已推送至 `origin/main`。
- 变更范围仅包含 DeepDoc Footer 后多余的 `deepdoc-tail` 节点、对应 CSS，以及本次证据台账与前后对比截图；公共 Header、Footer、Logo、预约弹窗、联系表单、Nginx、后台和数据库均未修改。
- 新发布包：`lingchen-website-18c13a8.tar.gz`，大小 41,339,730 bytes，SHA-256 为 `1f706db1281fefe7d68eb3ff30e6e9b2cc252e7e8fe7bde4971d2d452858c1c6`；服务器上传后复算一致。
- 本地增量门禁：`pnpm lint`、`pnpm exec tsc --noEmit`、`pnpm build`、`git diff --check`、`pnpm mastergo:check deepdoc` 全部通过；构建仍生成 74 个静态页面。
- 服务器暂存目录 `/home/lingchen/web/lingchen_website.release-18c13a8` 解压后共有 1,362 个正式文件，目录权限统一为 `0755`、文件权限统一为 `0644`；首页、联系页、DeepDoc、robots、sitemap、favicon 及文件可读性校验均通过。
- 切换前再次执行 Nginx 配置测试并通过。通过正式目录改名备份、暂存目录接管正式路径完成受控切换，未执行 Nginx reload。
- 本次旧站备份：`/home/lingchen/web/lingchen_website.backup-20260727-115147-18c13a8`。
- 服务器侧首页、DeepDoc、联系页、robots、sitemap、favicon、公共 Logo 与 `/system/contact-us` 均返回 HTTP 200；线上 DeepDoc HTML SHA-256 为 `96ed7eae102ff8b0bcce84136eaa07be7270cdb6cc7d7fa2ca993ec6d307688a`，与本地构建一致。
- 线上真实浏览器桌面视口 `1576 × 1258` 验证：唯一 H1、无 `.deepdoc-tail`、无横向溢出，21/21 图片完成解码，控制台 0 error；页面总高 `7237px`，Footer 底边 `7236.96875px`，确认 Footer 后不再存在额外空白。
- Header“立即体验”和面包屑“立即预约体验该产品”均打开同一公共预约弹窗；滚动锁定、Escape 关闭及焦点恢复通过。
- 本次增量未改动联系表单或 `/system/` 代理，因此未重复写入生产测试数据；沿用本记录上一批次已经完成的真实 POST 证据。
